import styled from 'styled-components'

export const Wrapper = styled.div`
    width: 100%;
    max-width: 960px;
    margin: 7rem auto;
`

export const SubTitle = styled.h2`
    font-size: 1.5rem;
    color: var(--gray);
`